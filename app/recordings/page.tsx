import React from "react";

enum ViewType {
  Recordings,
  Stream,
}

export default function RecordingsPage() {
  return (<>
    <h1>View Stream</h1>
    <div>
      <label> Stream ID </label>
    </div>
  </>);
}
